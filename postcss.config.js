/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    // Tailwind CSS v4 — PostCSS plugin (replaces the old tailwindcss plugin)
    "@tailwindcss/postcss": {},
  },
};

module.exports = config;
